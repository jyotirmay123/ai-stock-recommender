# Claude Code — Commands Authoring Guide

All files under `commands/` must stay **under 100 lines**.
Keep commands concise and crisp — use inline bullet lists, compress multi-step blocks, avoid prose padding.
